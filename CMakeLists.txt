cmake_minimum_required(VERSION 2.8)
project( roiSelector )
find_package( OpenCV REQUIRED )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable( roiSelector roiSelector.cpp )
target_link_libraries( roiSelector ${OpenCV_LIBS} )
